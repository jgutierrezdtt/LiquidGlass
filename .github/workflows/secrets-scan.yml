name: Secret Scanning (gitleaks)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  secrets:
    uses: jgutierrezdtt/Pipelines-Public/.github/workflows/reusable-secrets-scan.yml@main
    with:
      fail_on_findings: true
    secrets: inherit
